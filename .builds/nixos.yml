image: nixos/latest
repositories:
  nixpkgs: https://nixos.org/channels/nixpkgs-unstable
sources:
  - https://git.sr.ht/~jack/mudcore
tasks:
  - build: |
      cd mudcore
      nix-build --no-out-link
